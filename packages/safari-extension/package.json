{
  "name": "@vocably/safari-extension",
  "version": "0.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "build": "npm run build-prod --prefix ../extension-popup && rimraf dist && webpack --env production && npm run build-dev --prefix ../extension-popup ",
    "start": "webpack --watch --env development",
    "test": "echo '@vocably/extension has no tests.'",
    "extension": "npm start"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/vocably-pro/vocably.git"
  },
  "dependencies": {},
  "devDependencies": {}
}
